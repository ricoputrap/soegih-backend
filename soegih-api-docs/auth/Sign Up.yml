info:
  name: Sign Up
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/{{api_version}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "email": "admin2@gmail.com",
        "password": "password1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
